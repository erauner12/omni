apiVersion: omni.sidero.dev/v1alpha1
kind: MachineAction
metadata:
  generateName: install-
  namespace: default
spec:
  machineRef:                     # point at the *existing* Machine object
    name: 4913bb46-7cd8-2048-84c7-a8595f97a48c
    namespace: default
  install:
    disk: /dev/nvme0n1            # <— adjust if it’s sda, mmcblk0, etc.
    clusterRef:
      name: erauner-home
      namespace: default
    machineSetRef:
      name: erauner-home-control-planes
      namespace: default